/**
 * Just in case if the key that you gave to me reached
 * request limit, please use this new key (2dd138f8).
 * Key that you gave was(faf7e5bb).
 */

const KEY = 'faf7e5bb';

const CONFIG = {
  BASE_URL: `http://www.omdbapi.com/?apikey=${KEY}`,
};

export default CONFIG;
